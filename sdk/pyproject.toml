[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "controlplaneai-sdk"
version = "0.1.0"
description = "ControlPlane AI observability SDK"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.27"
]